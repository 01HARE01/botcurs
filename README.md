🏆 Cyber Esports Bot (Telegram Bot)

Этот Telegram-бот позволяет отслеживать матчи киберспорта в реальном времени с использованием **PandaScore API**.
Бот уведомляет о начале матчей, показывает ближайшие игры и отправляет ссылки на стримы (Twitch/YouTube).

🚀 Возможности бота

🎮 Поддерживаемые игры:

* League of Legends
* CS2
* Dota 2
* Valorant
* Rainbow Six Siege

🧩 Функционал:

* Просмотр ближайших матчей
* Просмотр подробностей по каждому матчу
* Подписка на уведомления по выбранной игре
* Автоматическое оповещение когда матч начинается
* Получение ссылки на стрим (Twitch / YouTube)
* Автоматическое преобразование времени под Алматы (UTC+6)
* Корректный парсинг ссылок стримов из любых форматов

📦 Установка и запуск

1. Клонировать репозиторий

```bash
git clone https://github.com/USERNAME/REPOSITORY.git
cd REPOSITORY
```

2. Установить зависимости

```bash
pip install -r requirements.txt
```

3. Создать файл `.env`

Создай в корне проекта файл:

```
.env
```

И добавь в него:

```
TOKEN=твой_telegram_bot_token
PANDA_SCORE=твой_pandascore_api_key
```

⚠ `.env` должен быть добавлен в `.gitignore`
чтобы токены не улетали в репозиторий.

▶️ Запуск бота

```bash
python main.py
```
Бот запустится и начнёт работать в режиме polling.
Отдельный поток запускает проверку матчей каждые 60 сек.

🗂 Структура проекта

```
📁 проект/
 ├── main.py          # основной код бота
 ├── requirements.txt # зависимости
 ├── .env             # токены (не загружается в github)
 └── README.md        # документация
```

⚙️ Используемые технологии

* Python 3.x
* Telebot (pyTelegramBotAPI)
* requests
* dotenv
* threading
* PandaScore API

🧠 Как работает уведомление о матчах

Отдельный поток (`check_matches()`) каждые 60 секунд:

1. Проверяет игры, на которые подписан пользователь
2. Получает список *running matches*
3. Если матч начался — отправляет уведомление (один раз)
4. Добавляет уникальный ключ в `_sent_notifications`

📩 Команды бота

 Команда         Описание                             
 `/start`        Главное меню                         
 `/matches`      Выбор игры → список ближайших матчей 
 `/alerts`       Подписка на уведомления              
 `/unsubscribe`  Отписаться от уведомлений            
